name: Auto Reply
on:
  issue_comment:
    types: [created]

jobs:
  reply:
    runs-on: ubuntu-latest
    steps:
      - name: Process comment
        run: echo "Received: ${{ github.event.comment.body }}"
